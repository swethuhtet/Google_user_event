<h1>Import CSV to Create Guests</h1>

<%= form_with url: import_guests_path, local: true, multipart: true do |form| %>
  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>
  <div class="actions">
    <%= form.submit 'Import' %>
  </div>
<% end %>
